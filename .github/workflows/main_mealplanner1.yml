# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Build and deploy JAR app to Azure Web App - mealplanner1

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
#          - uses: azure/login@v1
#            with:
#              creds: '$({ secrets.AZURE_CREDENTIALS })'
#
#          - uses:
#            with:
#              app-name: 'mealplanner2'
#              app-settings-json: '$({ secrets.APP_SETTINGS })'
#              connection-string-json: '$({ secrets.CONNECTION_STRINGS })'
#              general-setting-json: '{"alwaysOn": "false", "webSocketsEnabled": "true"}'
#            id: settings
#            - run: echo "The webapp-url is ${{ steps.settings.outputs.webapp-url }}"
#            - run: |
#                   az logout

    - uses: actions/checkout@v2

    - name: Set up Java version
      uses: actions/setup-java@v1
      with:
        java-version: '11'

    - name: Build with Maven
      run: mvn clean install

    - name: Upload artifact for deployment job
      uses: actions/upload-artifact@v2
      with:
        name: java-app
        path: '${{ github.workspace }}/target/*.jar'

  deploy:
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: 'production'
      url: ${{ steps.deploy-to-webapp.outputs.webapp-url }}

    steps:
    - name: Download artifact from build job
      uses: actions/download-artifact@v2
      with:
        name: java-app

    - name: Deploy to Azure Web App
      id: deploy-to-webapp
      uses: azure/webapps-deploy@v2
      with:
        app-name: 'mealplanner1'
        slot-name: 'production'
        publish-profile: ${{ secrets.AzureAppService_PublishProfile_03b36027a2d347e4bdf2cf3e71b82642 }}
        package: '*.jar'